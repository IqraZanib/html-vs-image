'use strict';
// Tanzania design pack — PLACEHOLDER. The design set (govt CBC template) is documented
// in DESIGN.md; no partner-approved visual style exists yet, so this pack applies no
// overrides: Tanzania renders use the locked default theme (RULES R26). Replacing this
// file's CSS is the whole job of shipping the TZ look.
const THEME_OVERRIDE_CSS = '';
module.exports = { THEME_OVERRIDE_CSS, REGION_NAME: 'Tanzania' };
