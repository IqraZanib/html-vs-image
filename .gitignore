# Dependencies
node_modules/

# Secrets — never commit API keys (.env, .env-api, etc.)
.env
.env.*
.env-api
*.env

# Generated output (root) — regenerated on every run
/lesson-plan.png

# ...but keep the sample image stored in assets/
!assets/

# Benchmark output
out/

# OS / editor
.DS_Store

# Large rendered reference pages (regenerable from the PDF)
references/source/page-*.png

# Large base64 build artifacts (regenerate via scripts/build-reference-repro.py)
references/samples/pinky-day-out-full.html
references/samples/pinky-day-out-exact.html
